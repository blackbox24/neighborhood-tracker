version: "3.8"

services:
  webserver:
    image: nginx:alpine
    container_name: nginx_web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html:ro
      - ./nginx/project.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
